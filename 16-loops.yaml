- name: Install pacakges
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print names
    ansible.builtin.package:
      name: "{{ item }}"  # item is a reserved keyword 
      state: latest
    loop:
    - nginx
    - mysql
    - zip